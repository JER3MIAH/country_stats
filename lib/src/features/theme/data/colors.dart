import 'package:flutter/material.dart';

var appColors = AppColors();

class AppColors {
  final black = Color(0xFFFFFFFF);
  final white = Color(0xFF000000);
  final darkBlue = Color(0xFF374151);
  final darkModeBackground = Color(0xFF293241);
  final lightModeText = Color(0xFF1E293B);
  final lightModeInput = Color(0xFF838383);
  final lightModeBackground = Color(0xFFFAFAFA);
}

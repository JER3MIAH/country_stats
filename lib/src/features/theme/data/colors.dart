import 'package:flutter/material.dart';

var appColors = AppColors();

class AppColors {
  final white = Color(0xFFFFFFFF);
  final black = Color(0xFF000000);
  final darkBlue = Color(0xFF2B3844);
  final darkModeBackground = Color(0xFF202C36);
  final lightModeText = Color(0xFF111517);
  final lightModeInput = Color(0xFF838383);
  final lightModeBackground = Color(0xFFFAFAFA);
}
